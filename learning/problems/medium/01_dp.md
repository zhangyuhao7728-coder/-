# ⭐⭐ 中等难度 - 动态规划

---

## 001. 最长递增子序列

**题目**: 给你一个整数数组 nums，找到其中最长严格递增子序列的长度。

**示例**:
```
输入: nums = [10,9,2,5,3,7,101,18]
输出: 4
解释: 最长递增子序列是 [2,3,7,101]，所以长度为 4
```

**代码模板**:
```python
def length_of_lis(nums: list[int]) -> int:
    # 在这里写你的代码
    pass
```

---

## 002. 最长公共子序列

**题目**: 给定两个字符串 text1 和 text2，返回这两个字符串的最长公共子序列的长度。

**示例**:
```
输入: text1 = "abcde", text2 = "ace"
输出: 3
解释: 最长公共子序列是 "ace"，长度为 3
```

**代码模板**:
```python
def longest_common_subsequence(text1: str, text2: str) -> int:
    # 在这里写你的代码
    pass
```

---

## 003. 完全平方数

**题目**: 给定正整数 n，找到若干个完全平方数（比如 1, 4, 9, 16, ...）使得它们的和等于 n。返回完全平方数最少数量。

**示例**:
```
输入: n = 12
输出: 3
解释: 12 = 4 + 4 + 4
```

**代码模板**:
```python
def num_squares(n: int) -> int:
    # 在这里写你的代码
    pass
```

---

## 进度追踪

| 题目 | 状态 | 难度 |
|------|------|------|
| 001 最长递增子序列 | ⬜ | ⭐⭐ |
| 002 最长公共子序列 | ⬜ | ⭐⭐ |
| 003 完全平方数 | ⬜ | ⭐⭐ |
